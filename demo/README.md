# Demo data

Run `claimkit demo` to generate five synthetic evidence images, EN/RU OCR sidecars,
an intentional model-number conflict, and a complete claim package. Generated files
are ignored by Git because they are reproducible.
